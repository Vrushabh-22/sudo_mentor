// Minimal stub for the standalone candidate portal build.
// The full ATS instance has an edge-function-backed WhatsApp prefs card; this
// renders nothing here because the standalone build doesn't expose that UI surface.
export function WhatsAppOptInCard(_props: any) {
  return null;
}
