// Tenant concept removed — this portal is single-tenant.
// Component intentionally renders nothing so existing imports keep working.
export function V4TenantSwitcher(_props: { onSwitched?: () => void }) {
  return null;
}
